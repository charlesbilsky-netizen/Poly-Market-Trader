*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
debug.keystore

# Release signing — never commit
my-upload-key.jks
*.jks
# EXCEPTION: the sideload keystore is committed deliberately. It only signs
# direct-install (non-Play) builds so every rebuild updates cleanly on-device.
!sideload-keystore.jks
